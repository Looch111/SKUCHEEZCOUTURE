export default function SectionLabel({ children }) {
  return (
    <span className="block font-sans text-[0.7rem] font-medium tracking-[0.32em] text-white/45 uppercase mb-1.5">
      {children}
    </span>
  );
}
